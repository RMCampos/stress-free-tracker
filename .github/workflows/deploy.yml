name: Build & Deploy to Pages

on:
  push:
    branches: [main]

env:
  APP_FOLDER: "stress-free-tracker"

jobs:
  build-deploy:
    runs-on: easynode-debian
    steps:
      - uses: actions/checkout@v4

      - name: Cache npm dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: npm run build

      - name: Install rsync + ssh client
        run: apt-get update -qq && apt-get install -y -qq rsync openssh-client

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.DEPLOY_HOST }}" >> ~/.ssh/known_hosts

      - name: Ensure destination folder exists
        run: |
          ssh -i ~/.ssh/deploy_key deploy@${{ secrets.DEPLOY_HOST }} \
            "mkdir -p /srv/sites/${{ env.APP_FOLDER }} && chmod 755 /srv/sites/${{ env.APP_FOLDER }}"

      - name: Upload site
        run: |
          rsync -avz --delete --exclude='.git*' --exclude='.gitea' \
            -e "ssh -i ~/.ssh/deploy_key" \
            ./ deploy@${{ secrets.DEPLOY_HOST }}:/srv/sites/${{ env.APP_FOLDER }}/
